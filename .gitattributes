# Normalize text files to LF in the repo (avoids CRLF issues on Linux/Apache)
* text=auto eol=lf

# Treat these as binary so git never touches their bytes
*.png   binary
*.jpg   binary
*.jpeg  binary
*.avif  binary
*.webp  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
